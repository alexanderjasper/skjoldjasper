# Cloudflare Tunnel token (find in CF Zero Trust → Networks → Tunnels → your
# tunnel → "Install and run a connector"). Treat as a credential — owns
# inbound traffic to every hostname routed through the tunnel.
TUNNEL_TOKEN=replace-me
