{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Start Database",
      "type": "shell",
      "command": "if ! docker info > /dev/null 2>&1; then echo 'Error: Docker is not running. Please start Docker/OrbStack and try again.'; exit 1; fi; if docker compose -f infra/docker-compose.yml ps postgres --format json 2>/dev/null | grep -q '\"State\":\"running\"'; then echo 'Database is already running.'; else docker compose -f infra/docker-compose.yml up -d postgres; fi",
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
        "panel": "dedicated",
        "focus": false
      },
      "runOptions": {
        "runOn": "default"
      }
    },
    {
      "label": "Stop Database",
      "type": "shell",
      "command": "docker compose -f infra/docker-compose.yml stop postgres",
      "problemMatcher": [],
      "presentation": {
        "reveal": "silent",
        "panel": "dedicated"
      }
    },
    {
      "label": "Check Database Status",
      "type": "shell",
      "command": "docker compose -f infra/docker-compose.yml ps postgres",
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
        "panel": "dedicated"
      }
    }
  ]
}

