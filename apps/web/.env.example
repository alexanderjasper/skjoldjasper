# Django
SECRET_KEY=change-me-in-dokploy-ui
DEBUG=False
ALLOWED_HOSTS=skjoldjasper.dk,localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=https://skjoldjasper.dk

# Database — defaults to a local SQLite file next to manage.py if unset.
# In production this points at the volume-mounted /data/db.sqlite3.
DATABASE_URL=sqlite:////data/db.sqlite3
